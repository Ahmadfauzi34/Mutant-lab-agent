[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mutant-lab-agent"
version = "0.1.0"
description = "Portable cognitive-agent runtime and lightweight mutant laboratory"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["agen_lab"]
