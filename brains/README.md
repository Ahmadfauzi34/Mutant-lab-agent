# Brains

Portable cognitive-state databases produced by training are product artifacts, not normal source files.

`.db`, `.sqlite3`, and `.agentckpt` files in this directory are ignored by Git by default. Store promoted brain packages in release/artifact storage or distribute them separately with an `AGENT_MANIFEST.json`.
