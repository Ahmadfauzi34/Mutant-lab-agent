# Incubator

Create local mutant workspaces here. Their contents are ignored by Git by default.

Each mutant should record at least:

- parent seed/runtime version;
- purpose;
- mutation/training class;
- datasets/sandboxes used;
- resulting brain artifact;
- evaluation result.

Only promoted, reusable definitions should leave the incubator.
